class Car {
  name: string;

  position: number;

  constructor(name: string) {
    this.name = name;
    this.position = 0;
  }

  // 차의 위치 get
  // 차 움직이는 함수

  // 차의 이름 get, set
}

export default Car;
